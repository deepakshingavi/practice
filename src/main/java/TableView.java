import javax.ws.rs.core.MediaType;

public class TableView {

    public boolean Issue(TableView tableViewInstance) {
        /*if (tableViewInstance != null && tableViewInstance.getItems().size() > 0) {
            for (int i = 0; i < tableViewInstance.getItems().size(); ++i) {
                ModelTO temp = (ModelTO) tableViewInstance.getItems().get(i);
                //  System.out.println(tableViewInstance.getId() + " " +temp);

                if (temp.getIssueStatus() != null){
                    switch (temp.getIssueStatus()) {
                        case "RED":
                            // red logic
                            break;
                        case "BLUE":
                            // blue logic
                            break;
                        case "GREEN":
                            // gree logic
                            break;
                        case "NA" : // NA logic
                            break;
                        case "ORANGE" :
                        case "PURPLE": // You can also combine logic for multiple colours
                            break;
                        default:
                            // If no match it this code invokes
                    }
                }
            }
        }*/
        return false;
    }
}

import java.time.LocalDateTime;
import java.time.format.DateTimeFormatter;
import java.util.concurrent.atomic.AtomicInteger;

public class AddDayInDate {

    final static AtomicInteger count = new AtomicInteger();

    public static void main(String[] args) throws ClassNotFoundException {
       /* count.set(1);
        count.incrementAndGet();
        count.decrementAndGet();
        DateTimeFormatter dtf = DateTimeFormatter.ofPattern("yyyy,MM,dd HH:mm");
        LocalDateTime now = LocalDateTime.now();
        String currentDTM = dtf.format(now);
        System.out.println(currentDTM);
        System.out.println(dtf.format(now.plusDays(2)));
        System.out.println(dtf.format(now.plusHours(6)));
        System.out.println(dtf.format(now.plusMinutes(15)));*/

//        System.out.println(new AddDayInDate[10]);
//        System.out.println(new byte[10][2]);

//        Class.forName("[LMyClass;");



    }
}

abstract class NestingEither {
  /*val a: Either[Value, Exception]
  val b: Either[Value, Exception]
  val c: Either[Value, Exception]

  def combine(a: Either[String, Exception],
              b: Either[String, Exception],
              c: Either[String, Exception]) = {
    if (a.isLeft && b.isLeft && c.isLeft) {
      a + b + c
    } else {
      Right(Seq(a, b, c).filter(_.isRight).head)
    }
  }*/


}
object NestingEither {


  def main(args: Array[String]): Unit = {



  }

}
